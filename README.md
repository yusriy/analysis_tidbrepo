# Analysis with TIDBRepo
This repo contains the code samples of analysis with TIDBRepo.
1. Upload data to TIDBRepo, including semantics and identifiers.

## Requirements
### Setup
```
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```
